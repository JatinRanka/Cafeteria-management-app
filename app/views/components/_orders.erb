<div class="orders">
  <% orders.each do |order| %>

    <p>
      <b><%= order.id%></b> -
      <%= order.is_delivered ? "Delivered" : "Pending" %>
    </p>

    <%= link_to "Order details",  "/orders/#{order.id}"%>

  <% end %>
</div>
